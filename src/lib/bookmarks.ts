export const bookmarks: any = {
    "user-1": [1, 2] // user-id: [prompt-id, prompt-id]
}


// get (check user is bookmark the prompt)

// post (bookmark) => add prompt into bookmark 

// api => fe  17:30 | 20:30 

// delete => remove from bookmark